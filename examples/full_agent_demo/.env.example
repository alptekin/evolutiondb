# Copy to `.env` next to docker-compose.demo.yml to enable real Claude.
# When ANTHROPIC_API_KEY is empty (or missing), the agent uses a
# deterministic stub LLM and the demo still runs end-to-end.

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

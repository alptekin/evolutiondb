name: Gmail Sync Release — PyPI

# Triggers on tag push `gmail-sync-v*` (e.g. `git tag gmail-sync-v0.1.1
# && git push --tags`). Distinct prefix so it does not collide with
# the other connector tags or the engine's `server-v*` / `v*` tags.
#
# What runs on the tag push:
#   1. Build sdist + wheel from client/gmail-sync
#   2. Upload to PyPI under the project name `evolutiondb-gmail-sync`
#      (Trusted Publishing via OIDC; falls back to PYPI_API_TOKEN
#      if the OIDC publisher is not yet configured).

on:
  push:
    tags: ["gmail-sync-v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing tag to re-publish (e.g. gmail-sync-v0.1.0)"
        required: true
  # The auto-tag workflow pushes the tag with the default GITHUB_TOKEN,
  # which by GitHub policy cannot trigger another workflow's `push`
  # event. Listen for the auto-tag completion instead so the release
  # chain runs end-to-end on a normal merge.
  workflow_run:
    workflows: ["Gmail Sync Auto-tag — push gmail-sync-v<version> on pyproject bump"]
    types: [completed]
    branches: [main]

permissions:
  contents: read
  id-token: write   # PyPI Trusted Publishing needs this

jobs:
  pypi:
    name: Publish evolutiondb-gmail-sync to PyPI
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'push' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' &&
       github.event.workflow_run.conclusion == 'success')
    environment:
      name: pypi-gmail-sync
      url: https://pypi.org/project/evolutiondb-gmail-sync/
    steps:
      - uses: actions/checkout@v4
        with:
          # workflow_run fires from `main` (the auto-tag's branch) and
          # carries no tag ref — derive the tag from pyproject below.
          ref: ${{ github.event.inputs.tag || github.ref }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Verify version matches the tag
        working-directory: client/gmail-sync
        run: |
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            ver=$(python3 -c "import tomllib;print(tomllib.loads(open('pyproject.toml','rb').read().decode())['project']['version'])")
            tag="gmail-sync-v${ver}"
          elif [ -n "${{ github.event.inputs.tag }}" ]; then
            tag="${{ github.event.inputs.tag }}"
            ver="${tag#gmail-sync-v}"
          else
            tag="$GITHUB_REF_NAME"
            ver="${tag#gmail-sync-v}"
          fi
          py_ver=$(python3 -c "import tomllib;print(tomllib.loads(open('pyproject.toml','rb').read().decode())['project']['version'])")
          if [ "$ver" != "$py_ver" ]; then
            echo "::error::tag ${tag} (=> ${ver}) does not match pyproject version ${py_ver}"
            exit 1
          fi
          echo "tag and pyproject agree on ${ver}"

      - name: Build sdist + wheel
        working-directory: client/gmail-sync
        run: |
          python3 -m pip install --upgrade pip build
          rm -rf dist build *.egg-info
          python3 -m build --sdist --wheel
          ls -lh dist/

      - name: Verify the wheel installs and the entry point runs
        working-directory: client/gmail-sync
        run: |
          python3 -m venv /tmp/install-check
          /tmp/install-check/bin/pip install --upgrade pip
          /tmp/install-check/bin/pip install dist/evolutiondb_gmail_sync-*.whl
          /tmp/install-check/bin/evolutiondb-gmail-sync --help

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: client/gmail-sync/dist
          # If PYPI_API_TOKEN is set as a repo secret, use it; otherwise
          # the action falls back to PyPI Trusted Publishing via the
          # workflow's OIDC token (configure the pending publisher at
          # https://pypi.org/manage/account/publishing/).
          password: ${{ secrets.PYPI_API_TOKEN }}
          skip-existing: true
